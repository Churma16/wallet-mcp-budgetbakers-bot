# ==============================================================================
# AI Provider Configuration (gemini | openrouter | groq | ollama | openai | custom)
# ==============================================================================
# Choose your active AI Provider (default: gemini)
AI_PROVIDER=gemini

# --- 1. Google Gemini (Default) ---
# Obtain free API key at: https://aistudio.google.com
GEMINI_API_KEY=your_gemini_api_key_here

# Primary Model:
# - gemini-3.5-flash-lite (Recommended: ultra-fast 1-2s response, lightweight, low latency)
# - gemini-3.5-flash / gemini-3.6-flash (Higher reasoning, slightly higher latency 5-10s)
GEMINI_MODEL=gemini-3.5-flash-lite

# Fallback models when primary model is busy (503) or rate-limited (429)
GEMINI_FALLBACK_MODELS=gemini-3.5-flash,gemini-3.6-flash
GEMINI_TIMEOUT_SECONDS=20

# --- 2. OpenRouter (Optional: support free & paid models) ---
# OPENROUTER_API_KEY=sk-or-v1-your_openrouter_key_here
# Examples of multimodal free models on OpenRouter: google/gemini-2.0-flash-exp:free, qwen/qwen-2.5-vl-72b-instruct:free
# AI_MODEL=google/gemini-2.0-flash-exp:free
# AI_FALLBACK_MODELS=meta-llama/llama-3.3-70b-instruct:free,deepseek/deepseek-r1:free

# --- 3. Groq / Ollama / OpenAI / Custom (Optional) ---
# GROQ_API_KEY=gsk_your_groq_key_here
# OPENAI_API_KEY=sk-your_openai_key_here
# AI_BASE_URL=http://localhost:11434/v1
# AI_TIMEOUT_SECONDS=25

# BudgetBakers Wallet MCP Server Configuration
# Base URL for Wallet MCP
WALLET_MCP_BASE_URL=https://mcp.wallet.budgetbakers.com
# Personal Access Token from https://web.budgetbakers.com/settings/mcp-server
WALLET_MCP_ACCESS_TOKEN=your_wallet_mcp_personal_access_token_here

# Messaging Channels (Optional: comma-separated 'whatsapp,telegram', defaults to auto-detect based on provided credentials)
# ENABLED_MESSENGER_CHANNELS=whatsapp,telegram

# WhatsApp Configuration (Required if WhatsApp channel is enabled)
# Phone number allowed to interact with the bot (international format without '+' or spaces)
# Example: 6281234567890
ALLOWED_PHONE_NUMBER=6281234567890

# Path to store WhatsApp session credentials
WHATSAPP_SESSION_PATH=./auth_session

# Telegram Configuration (Required if Telegram channel is enabled)
# Obtain Bot Token from @BotFather on Telegram
# TELEGRAM_BOT_TOKEN=123456789:ABCdefGhIJKlmNoPQRsTUVwxyZ
# Your personal Telegram User ID or Username (whitelist security). Obtain ID from @userinfobot or @raw_data_bot
# TELEGRAM_ALLOWED_USER_ID=123456789

# File Logging Retention Policy (number of days to keep daily log files in logs/)
LOG_RETENTION_DAYS=7

# Email Bank Synchronization (Gmail IMAP IDLE) - Optional
# Set to 'true' to enable real-time bank and e-wallet email tracking (default: false)
EMAIL_SYNC_ENABLED=false
EMAIL_IMAP_HOST=imap.gmail.com
EMAIL_IMAP_PORT=993
EMAIL_IMAP_USER=your_email@gmail.com
# Google App Password (16-character password generated from Google Account > Security > 2-Step Verification > App Passwords)
EMAIL_IMAP_PASSWORD=your_16_char_google_app_password
# Minutes to look back on initial startup (default: 10 minutes) to prevent flood
EMAIL_LOOKBACK_MINUTES=10
